# extra tooling needed by the pytest-conda-solvers workflow specifically:
# `conda pypi install -e` (unlike plain `pip install -e`) does not do PEP 517
# build isolation or resolve dependencies, so both conda-pycosat-solver's
# build backend and pytest-conda-solvers' own runtime requirements need to be
# pre-installed explicitly.

# conda-pycosat-solver's build backend (see pyproject.toml [build-system])
setuptools >=71.0
setuptools-scm

# pytest-conda-solvers' own runtime requirements
coverage
fastapi
fastapi-cache2
httpx
msgspec
pytest
pytest-cov
pytest-split
pytest-xprocess
typer
uvicorn
xdoctest
